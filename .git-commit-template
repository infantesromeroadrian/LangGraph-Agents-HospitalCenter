chore: update gitignore and format tests with ruff

Changes to .gitignore:
- Add .cursor/ to ignored directories (IDE files)
- Add historyMD/, tickets/, tracking/ (local documentation)
- Add langgraph-medicalcenter-venv/ (old venv)
- Reorganize sections with clear headers
- Remove .dockerignore from gitignore (should be committed)
- Add .flake8 to linting ignores
- Add more comprehensive OS-specific ignores

Changes to tests/:
- Auto-formatted with ruff (import sorting, spacing)
- No functional changes
- All tests remain unchanged logic-wise

Critical verifications:
✅ .env is ignored (contains secrets)
✅ .cursor/ is ignored (IDE config)
✅ historyMD/ is ignored (local docs)
✅ logs/ is ignored
✅ .venv/ is ignored

What will be committed:
- .gitignore (improved)
- tests/__init__.py (formatted)
- tests/test_agents.py (formatted)
- tests/test_graph.py (formatted)
- tests/test_memory.py (formatted)
